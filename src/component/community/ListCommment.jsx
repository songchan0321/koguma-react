import { Fragment } from "react";
import GetComment from "./GetComment";

const ListComment = () => {
  return (
    <Fragment>
      <GetComment />
    </Fragment>
  );
};

export default ListComment;
